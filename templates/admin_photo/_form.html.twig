{{ form_start(form) }}
    {{ form_widget(form) }}
<button class="btn btn-primary my-1">
    {{ button_label|default('<i class="fa fa-floppy-o" aria-hidden="true"></i>' ) | raw }}
</button>
{{ form_end(form) }}

