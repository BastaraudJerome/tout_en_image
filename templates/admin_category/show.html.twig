{% extends 'base-admin.html.twig' %}

{% block title %}Category{% endblock %}

{% block body %}
    <h1>Category</h1>
        <a class="btn btn-primary" href="{{ path('admin_category_index') }}">
            Retour
        </a>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ category.type }}</td>
            </tr>
        </tbody>
    </table>




    <a href="{{ path('admin_category_edit', {'id': category.id}) }}">
        <i class="fa fa-pencil btn btn-primary-edit my-1" aria-hidden="true"></i>
    </a>


    {{ include('admin_category/_delete_form.html.twig') }}
{% endblock %}
