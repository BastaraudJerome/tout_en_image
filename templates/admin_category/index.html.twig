{% extends 'base-admin.html.twig' %}

{% block title %}Category index{% endblock %}

{% block body %}
    <h1>Category index</h1>
<a class="btn btn-primary" href="{{ path('admin_category_new') }}">
   Nouveau
</a>


    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Type</th>
                
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for category in categories %}
            <tr>
                <td>{{ category.id }}</td>
                <td>{{ category.type }}</td>
                <td>
                    <a href="{{ path('admin_category_show', {'id': category.id}) }}"><i class="fa fa-eye btn btn-primary-show" aria-hidden="true"></i></a>
                    <a href="{{ path('admin_category_edit', {'id': category.id}) }}"><i class="fa fa-pencil btn btn-primary-edit" aria-hidden="true"></i></a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">Aucunes Données</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    
{% endblock %}
